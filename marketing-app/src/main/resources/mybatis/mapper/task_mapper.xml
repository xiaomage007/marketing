<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.charlie.infrastructure.persistent.dao.ITaskDao">

    <resultMap id="dataMap" type="com.charlie.infrastructure.persistent.po.Task">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="message_id" property="messageId"/>
        <result column="exchange" property="exchange"/>
        <result column="routing_key" property="routingKey"/>
        <result column="queue" property="queue"/>
        <result column="message" property="message"/>
        <result column="state" property="state"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <insert id="insert" parameterType="com.charlie.infrastructure.persistent.po.Task">
        insert into task(
            user_id, message_id, exchange, routing_key, queue, message, state, create_time, update_time
        )
        values(
            #{userId}, #{messageId}, #{exchange}, #{routingKey}, #{queue}, #{message}, #{state}, now(), now()
        )
    </insert>

    <update id="updateTaskSendMessageCompleted" parameterType="com.charlie.infrastructure.persistent.po.Task">
        update task set state = 'completed', update_time = now()
        where user_id = #{userId} and message_id = #{messageId}
    </update>

    <update id="updateTaskSendMessageFail" parameterType="com.charlie.infrastructure.persistent.po.Task">
        update task set state = 'fail', update_time = now()
        where user_id = #{userId} and message_id = #{messageId}
    </update>

    <select id="queryNoSendMessageTaskList" resultMap="dataMap">
        select id, user_id, message_id, exchange, routing_key, queue, message, state, create_time, update_time
        from task
        where state = 'fail' or (state = 'create' and now() - update_time > 6)
        limit 10
    </select>

</mapper>
